interface:
  display_name: "Verify Browser UI"
  short_description: "Inspect Windows browser UI with real UX-flow verification"
  default_prompt: "Use $verify-browser-ui to inspect a local dev page desktop-first, click through real navigation, verify screen transitions, exercise safe edit and drag flows when present, run a mobile confirmation pass, and delete session artifacts before finishing."
