Req: Investigate how to make daily row resizing and grid interaction feel closer to Google Sheets / Excel by reviewing current code and public high-performance spreadsheet patterns.
Local: Confirmed `TaskWorkspace` still renders a full desktop `<table>` with `dailyTreeRows.map(...)`, 15 columns per row, per-cell DOM/editor structure, no virtualization library usage, and only the drag path itself is optimized via DOM fast-path.
Research: Google Sheets public sources describe calc work happening in a Web Worker and recent WasmGC migration for faster computation; public grid references consistently emphasize viewport virtualization, buffered rendering, or canvas rendering for spreadsheet-class responsiveness.
Recommendation: Near-term move from full table rendering to viewport-driven row virtualization plus granular external-store subscriptions; medium-term separate display layer from editor layer; long-term adopt a canvas-backed grid body if the target is true Sheets/Excel-level interaction smoothness.
Verify: Reviewed local files `src/components/tasks/task-workspace.tsx`, `src/domains/task/daily-list.ts`, `package.json`; checked public sources from Google Workspace, web.dev, React docs, TanStack Virtual, AG Grid, Glide Data Grid, Chrome Developers, and MDN.
