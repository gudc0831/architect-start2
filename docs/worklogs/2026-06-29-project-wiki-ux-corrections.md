# 2026-06-29 Project WIKI UX Corrections

Diff: Task Assistant Project WIKI registration preview now allows editing title, summary, body Markdown, and tags before registration. Registration sends the edited draft with the source ids and supplemental note, and the API accepts edited fields either top-level or nested under `draft`. Temporary review history now renders the newest 6 question/run-context groups with version counts instead of a flat recent-record list. Project WIKI success and linked common-candidate source records use the explicit `공용WIKI 후보 생성` state. Project WIKI status controls now allow only creator, project manager, or admin changes, require a disable reason, and keep restore reason optional.

Validator updates: `task-assistant:unified:validate`, `project-wiki:validate`, `project-wiki:behavior:validate`, and `project-wiki:preview-smoke` now check the editable preview, grouped temporary history/version count, explicit common-candidate state copy, edited-draft registration contract, and disable reason/authorization UX.

Verification: `npm run task-assistant:unified:validate`; `npm run project-wiki:validate`; `npm run project-wiki:behavior:validate`; `PROJECT_WIKI_SMOKE_URL=http://127.0.0.1:3024/preview/materials?view=wiki npm run project-wiki:preview-smoke`; Task Assistant preview browser flow through generate, approve, editable Project WIKI registration, and `공용WIKI 후보 생성`; `npm run typecheck`; `npm run lint`; `npm run worklog:check`; `git diff --check`; `npm run build` passed on 2026-06-29 KST. Commit, push, deployment, and deployed-browser evidence are pending in this rollout.
