Req: implement the local-first daily-grid responsiveness plan so quick create, inline edit, and row resize react immediately on `/daily`, then confirm the same shared V2 path on `/preview/daily`.
Diff: split quick-create and inline-edit hot paths out of top-level `TaskWorkspace` state, added store-backed DOM/editor/row-metrics helpers plus `DailyGridHeaderV2` and `DailyGridBodyV2`, and updated `src/app/globals.css` / `src/components/tasks/task-workspace.tsx` to wire the virtualized V2 grid behind the local/preview shared daily path.
Why: reduce top-level rerender fan-out during typing, move row-resize feedback onto a transient metrics + virtualizer path, and keep preview parity without introducing a separate preview-only fork.
Verify: `npm run typecheck`, `npm run build`, local browser QA on `http://127.0.0.1:3000/daily` for quick create typing, inline edit commit/cancel, row-resize persistence and reload restore, plus preview QA on `http://127.0.0.1:3000/preview/daily` confirming the V2 grid path, overlay open/cancel, and zero console errors; residual risk is that the earlier row-resize handle hit-test timeout was not reproduced again in the final verification pass.
