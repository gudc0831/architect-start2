Req: Add an official Korean holiday API path with local fallback, file-backed month caching, preview no-call behavior, and deployment-safe guards for invalid input and oversized ranges.
Diff: Added `src/app/api/calendar/holidays/route.ts`, `src/lib/tasks/calendar-holiday-service.ts`, `src/lib/tasks/calendar-holiday-types.ts`, extended `src/lib/tasks/korean-public-holidays.ts`, wired holiday loading in `src/components/tasks/task-workspace.tsx`, and exposed `HOLIDAY_API_SERVICE_KEY` plus cache-root guidance in `src/lib/runtime-config.ts` and `.env.example`.
Why: The calendar should prefer authoritative holiday data when available, but still render correctly in preview/local/offline mode and stay safe on deployed Node/serverless environments with limited or ephemeral filesystem access.
Verify/Time: `npm run typecheck` passed; targeted `npx eslint` on the changed files passed; `node -` against `/api/calendar/holidays` returned `200` for May 2026 plus `400` for invalid and oversized ranges; `npx tsx` smoke tests confirmed `api -> cache` reuse and tolerated cache-write failure; browser snapshot on `/calendar?month=2026-05` confirmed Sunday-first headers, month controls, and holiday tinting | 2026-04-06 KST
