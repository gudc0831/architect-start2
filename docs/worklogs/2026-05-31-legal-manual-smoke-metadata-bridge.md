Req: Support the verified-legal manual smoke handoff by carrying source locator and confidence reason through the user-facing assistant generation path.
Diff: Added legal `locator` and `confidenceReason` metadata to assistant evidence, preserved/sanitized those fields during verified legal search mapping and assistant-record storage normalization, and included legal metadata in SaaS assistant prompt evidence summaries.
Why: The five legal manual smoke answers must be able to show source title, source kind, authority rank, effective date or stale warning, source URL or locator, and confidence reason.
Verify/Time: 2026-05-31 KST; RED `npm.cmd run legal-search:validate` failed on missing locator/confidenceReason metadata, secret-bearing confidenceReason redaction, missing fallback locator, malformed generated retrieval handling, stored legal metadata validation/redaction, unredacted chunkId, missing answer-notice fallback, warning source-id leakage, locator-key leakage, storage URL mixed-case OC handling, and embedded locator-key OC handling; GREEN passed with 72 cases; `npm.cmd run task-review:validate`; `npm.cmd run assistant-thread-memory:validate`; `npm.cmd run legal-change-monitor:validate`; `npx.cmd tsc --noEmit --incremental false`. `npm.cmd run typecheck` still hits the existing `tsconfig.tsbuildinfo` EPERM write issue.

Req: Add the missing execution bridge for the five real manual-smoke assistant answers.
Diff: Added `src/domains/assistant/legal-manual-smoke-report.ts`, `scripts/legal-manual-smoke-capture.ts`, and `npm.cmd run legal-manual-smoke:validate` / `npm.cmd run legal-manual-smoke:capture`. The capture CLI converts saved `/api/assistant/generate` outputs into the verified manual-smoke report shape.
Why: Task 10 Step 3 still requires a real `<report.json>` that can be validated by `verified-legal-evidence-api` with `npm.cmd run smoke:legal:manual-template -- --validate <report.json>`.
Verify/Time: 2026-05-31 KST; RED `npm.cmd run legal-manual-smoke:validate` failed on the missing report module, invalid top-level report state passing validation, mismatched derived report status passing validation, and spaced `OC = ...` credential-like text passing validation; GREEN passed with 11 cases. A sample capture JSON containing a UTF-8 BOM was converted by `npm.cmd run legal-manual-smoke:capture -- --input <captures.json> --output <report.json>`, then the generated report passed `npm.cmd run smoke:legal:manual-template -- --validate <report.json>` in `verified-legal-evidence-api` with 5 cases. Also passed: `npm.cmd run legal-search:validate` (72 cases), `npm.cmd run task-review:validate`, `npm.cmd run assistant-thread-memory:validate` (7 cases), `npm.cmd run legal-change-monitor:validate`, `npm.cmd run legal-batch-audit:validate` (20 cases), and `npx.cmd tsc --noEmit --incremental false`.

Req: Complete the actual five-question user-facing assistant smoke capture.
Diff: Added `scripts/legal-manual-smoke-generate-capture.ts` and `npm.cmd run legal-manual-smoke:generate-capture`. The script calls an already running architect-saas HTTP app, configures mock assistant policy through `/api/admin/assistant/policy`, creates five smoke tasks through `/api/tasks`, calls `/api/assistant/generate`, builds the manual-smoke report, and validates it before writing.
Why: Direct route-handler invocation is not enough because Next request-scoped APIs such as `cookies()` require a real HTTP request scope. The smoke must exercise the user-facing API route.
Verify/Time: 2026-05-31 KST; actual HTTP run passed. Generated report: `D:\architect-workspace\verified-legal-evidence-api\data\staging\manual-smoke\assistant-generate-report-20260531T000000Z.json`. Verified validator returned `status: "passed"`, `caseCount: 5`, `failures: []`. Report summary: all five canonical cases cited, no blank required fields, and no credential-like markers found.
