Req: continue the `codex/multi-user-transition` auth slice by making the post-login no-access state behave like a dedicated auth screen instead of falling through the normal project shell, and let users switch accounts cleanly from that state.
Diff: bypass `ProjectShell` for `/auth/*` pages in `src/components/layout/app-shell.tsx`; add `src/components/auth/no-access-actions.tsx` to perform same-site logout and return to `/login`; update `src/app/auth/no-access/page.tsx` to show the signed-in email plus the new sign-out action; add small `empty-state` support styles in `src/app/globals.css`.
Why: the locked auth contract calls for a dedicated no-access/access-pending state after successful sign-in. Leaving `/auth/no-access` inside the regular workspace shell makes the state feel like a broken workspace and makes account switching harder than it should be.
Verify: `npm run typecheck` passed; direct `.\node_modules\.bin\eslint.cmd` on `src/components/auth/no-access-actions.tsx`, `src/components/layout/app-shell.tsx`, and `src/app/auth/no-access/page.tsx` passed.
Risks: no browser run was performed here, so the visual/auth flow still needs preview-cloud verification; copy for the no-access screen remains hardcoded and can be localized later if this screen becomes a longer-lived user-facing surface.
