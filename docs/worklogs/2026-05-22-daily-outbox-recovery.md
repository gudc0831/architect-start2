Req: Stabilize `/daily` server sync behavior without changing the existing spreadsheet-like optimistic UI or requiring Preview URL verification in this slice.
Diff: Added classified `/daily` mutation journal failure metadata, retryable pending recovery for network/database errors, update/reorder `409` rebase helpers, trash/delete desired-state `404` reconciliation, immediate journal refresh after create acknowledgement for temp-id follow-up operations, idempotent no-op handling for already-trashed task moves, status copy for sync waiting, and regression coverage in `scripts/daily-editing-responsiveness-verify.ts`.
Why: A stale or transient failed outbox operation should not make the page look like the server is down, and spreadsheet-style editing should recover by retrying/rebasing local operations against fresh server state before surfacing a terminal sync failure.
Verify/Time: 2026-05-22 KST; `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed.
