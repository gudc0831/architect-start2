Req: Implement the first Task Assistant Core Loop SaaS foundation for the browser assistant slice.
Diff: Added `docs/assistant-extension-contract.md`, assistant domain/repository/use-case modules, `/api/assistant/task-context`, `/api/assistant/retrieve`, `/api/assistant/records`, `/api/assistant/summaries`, Prisma assistant record/summary models and migration, local data-guard support for assistant records, RLS policies for assistant tables, and configured Chrome extension origin allowlist support via `ARCHITECT_ASSISTANT_EXTENSION_ORIGINS`.
Why: The browser assistant needs a SaaS-owned API boundary for task context, permission-checked retrieval, assistant record persistence, and work summary drafts without depending on DB internals.
Verify/Time: 2026-05-07 16:05 KST; ran `npm run db:generate`, `npm run typecheck`, `npm run lint`, and `npm run build`. Build passed with pre-existing data-guard dynamic file-pattern warnings. Harness reviewer P1 findings for extension origin and RLS were addressed.
