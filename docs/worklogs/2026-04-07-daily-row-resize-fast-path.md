Req: Make daily row-height resize feel spreadsheet-fast so dragging updates only the active row immediately instead of pushing the whole table through React on every pointer move.
Diff: `src/components/tasks/task-workspace.tsx` now keeps row-resize drag on a DOM fast path by updating only the active row cell shells during `pointermove`, commits `TaskListRowHeightMap` once on `pointerup`, and reuses `EMPTY_TASK_FILES` for daily-row file props so memoized rows without files stop receiving fresh array references each parent render.
Why: Remove the main drag-time render bottleneck and preserve row-level memo boundaries so row-height interactions behave closer to Google Sheets than to a full-table React rerender.
Verify/Time: `npm run typecheck` (pass); `npm run build` (pass, same 2 unrelated Turbopack `data-guard` warnings); headed Playwright QA on `http://127.0.0.1:3000/preview/daily` confirmed row `task-101` grew from `52px` to `112px` during drag while sibling row `task-102` stayed `52px`, and preview `localStorage` stayed unchanged mid-drag; Playwright mouse-up did not provide a reliable persistence proof for the preview drag path, so pointer-up storage remains code-reviewed but not fully browser-proven in this run / 2026-04-07.
