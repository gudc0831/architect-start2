Req: Finish preview cloud verification for admin/member/no-access paths, invalid redirect handling, protected diagnostics, and decide the fate of the transitional password login route.
Diff: verified preview Google OAuth against real Supabase accounts for `admin`, `member`, and `no-access` outcomes; confirmed invalid external `next` falls back to an internal route; confirmed unauthenticated `/api/system/status` returns `UNAUTHORIZED`; kept `src/app/api/auth/login/route.ts` as a controlled utility but restricted it with same-site request-integrity checks and a production/preview-off default behind `ALLOW_TRANSITIONAL_PASSWORD_LOGIN`.
Why: preview verification is now strong enough to stop treating password login as a normal cloud entry path, but local/bootstrap recovery still benefits from a narrowly scoped fallback utility.
Verify: preview browser checks for `gudc083111@gmail.com`, `gudc08311@gmail.com`, and `gudc0831111@gmail.com`; `curl.exe -I https://architect-start2-git-codex-multi-d1c003-chois-projects-7b2948cf.vercel.app/login`; `npm run typecheck`; `npm run build`.
