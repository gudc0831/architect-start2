Req: Prepare a practical way to test all current user-facing service functions, including task creation/editing/content changes/delete, AI review, upload, export, and related flows.
Diff: Added `docs/2026-06-29-user-feature-test-matrix.md` with execution gates, disposable test data rules, browser E2E matrix, first live QA pass, and approval template. After browser QA found local-mode GET failures, added non-cloud GET fallbacks for project materials uploads, access requests, and invitations so local-file mode read screens do not call Prisma without `DATABASE_URL`. Write-path QA then found export verification failed when optimistic create persisted `siblingOrder: -1`; patched `createTask` to rebase negative create orders to non-negative sibling order values while keeping the new row at the top.
Verify/Time: Ran local baseline validators on 2026-06-29 KST. Passed `typecheck`, `daily:editing:verify`, `daily:cell-collaboration:verify`, `task-review:validate`, `task-assistant:unified:validate`, `project-wiki:validate`, and `project-wiki:behavior:validate`. Browser read-only pass on `http://127.0.0.1:3024` confirmed `/daily`, AI panel open, `/board`, `/calendar`, `/trash`, `/ai-settings`, `/materials`, and `/admin`; local-mode 503 findings were fixed and rechecked. Browser write run on `http://localhost:3024` created, edited, uploaded a file, exported, opened AI review, trashed/restored, and permanently cleaned up `qa-20260629-1614` records. Post-fix export verification passed with 5 rows before cleanup and 3 rows after cleanup.
Blocked/Deferred: AI review generation was not executed because `ai-review:readiness` is blocked by missing `VERIFIED_LEGAL_SEARCH_API_URL`; Browser Assistant bridge remains disconnected in this local session. `daily:navigation-pending-sync:verify` still needs an explicit `--url`; `ui-copy:validate` passed typecheck/lint/build but failed its copy-status gate because `output/ui-copy/translator-status.json` is missing.
Next: Run provider-backed AI generation and Browser Assistant/Local Codex review only after the legal search URL and extension bridge are available on the chosen local or Preview target.
