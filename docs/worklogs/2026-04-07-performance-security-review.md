Req: Review the current project for structural performance bottlenecks and security gaps, then implement the highest-value safe fixes.
Diff: Recorded the review outcome; wrapped `src/components/layout/app-shell.tsx` with the existing `DashboardProvider`; added `src/use-cases/project-scope-guard.ts`; updated `src/use-cases/file-service.ts` and `src/use-cases/trash-service.ts` to enforce selected-project scope; adjusted `src/lib/auth/auth-config.ts` and `src/lib/auth/require-user.ts` so unsafe non-cloud production fails closed without breaking static builds.
Why: Tab switches were still remounting dashboard data at the shell boundary, file and trash mutations trusted raw ids across projects, and non-cloud auth fallback in production was still a deployment footgun.
Verify/Time: `npm run typecheck` pass; `npm run lint` pass with the same 3 existing warnings in `src/components/tasks/task-categorical-fields.tsx`; `npm run build` pass; browser check on 2026-04-07 confirmed `/daily -> /board -> /calendar -> /daily?taskId=arch-task-092` and mobile `/preview/daily` still render correctly.
