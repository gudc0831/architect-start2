Req: Add the code foundation for future multi-theme switching without changing current layout or visible styling, including user preference persistence, runtime theme application, and a shared sidebar entry point.
Diff: Added theme preference domain/types, repository/use-case support, `GET/PATCH /api/preferences/theme`, `ThemeProvider`, sidebar `ThemeSelector`, Prisma `profile_preferences.theme_id` schema+migration, and semantic theme token scaffolding in `src/app/globals.css` plus admin CSS token adoption in `src/components/admin/admin-foundation-shell.module.css`.
Why: The UI was locked to a single green palette through scattered CSS values and had no persisted theme setting, so later theme work would have required structural rewrites instead of simple token overrides.
Verify/Time: `npm run db:generate` pass; `npm run typecheck` pass; `npm run lint` pass with the same 3 existing warnings in `src/components/tasks/task-categorical-fields.tsx`; `npm run build` pass with the same pre-existing Turbopack `data-guard` pattern warnings only / 2026-04-07.
