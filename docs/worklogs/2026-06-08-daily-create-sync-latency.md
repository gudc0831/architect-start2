Req: Reduce `/daily` task-create save sync latency in priority order after Preview evidence showed tab navigation was fixed locally but save acknowledgment still waited on slow backend create.
Diff: Changed the create-task category context load from per-field global/project repository calls to one bulk global query plus one bulk project query, then resolved each field in memory. Added `daily:create-latency:harness` to keep this route from regressing to field fanout.
Why: Exact Preview timing evidence put `service.effectiveCategories` at roughly 2.2-2.4s of the 5.1-5.3s create round trip, with repository timings showing each remote DB round trip costs about 200ms. Reducing repeated category queries is the highest-priority backend latency win before more invasive repository/auth changes.
Verify/Time: `npm run daily:create-latency:harness`, `npm run daily:editing:verify`, `npm run typecheck`, `npm run lint`, `npx prisma validate`, `git diff --check`, `npm run worklog:check`, and `npm run build` passed locally before and after the create-path correction on 2026-06-08 KST. `git diff --check` printed only Windows LF-to-CRLF working-copy normalization notices for existing tracked files and exited 0.
Failure: First deployed measurement after commit `5ad8eb417a3ed681deb8d8d42c3a307e5a6bce23` still showed `service.effectiveCategories` at 2237ms and total `POST /api/tasks` at 7841ms on the exact `/daily` Preview URL.
Cause: The initial change optimized the admin/session category resolver, but `/api/tasks` create uses `task-service.ts` `loadEffectiveTaskCategories()`, which still called `listEffectiveTaskCategoryDefinitions()` separately for every field.
Fix: Moved the same bulk global/project category lookup into the actual create-task service path and extended `daily:create-latency:harness` to assert the create path cannot call per-field effective category repository methods.
Diff: Added a selected-project access fast path for route guards. When the project session cookie is present, non-admin users now load the selected project and membership in one repository call before falling back to the older full project-selection flow.
Why: After the create-path category fix, exact Preview timing showed `service.effectiveCategories` reduced to 804ms and `route.createTask` to 2227ms, but total `POST /api/tasks` still took 6992ms because `route.requireUser` was 2385ms and `route.projectEditorGuard` was 1091ms. The guard fast path targets the project guard portion without weakening the existing read/editor permission checks.
Diff: Changed `requireUser()` to try Supabase `getClaims()` first and keep the existing `getUser()` path as fallback.
Why: Exact Preview timing after the guard fast path showed `route.projectEditorGuard` reduced to 411ms, but `route.requireUser` still took 1981ms. Supabase Auth documents `getClaims()` as the faster verified-JWT path, falling back internally to `getUser()` when local JWKS verification cannot be used.
Diff: Combined repository advisory locking and next task-number lookup into one SQL round trip, and skipped the existing-id lookup when the server generated the task id.
Why: Exact Preview timing after the auth change showed `route.requireUser` reduced to 1575ms, but `service.repositoryCreate` still took 1427ms with sequential remote DB round trips. Lock and max-number lookup must remain serialized, but they do not need separate network round trips.
Diff: Increased the sidebar route fallback delay from 150ms to 1000ms and added a pending-sync navigation verifier.
Why: Browser verification with a delayed `/api/tasks` create showed the sidebar fallback could force a full document load before the App Router completed a same-document transition. That made tab movement feel slow specifically while the daily create sync was pending.
