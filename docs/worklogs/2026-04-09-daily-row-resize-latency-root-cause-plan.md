Req: Review the spreadsheet-grid handoff docs and current code to explain why task-row height resize still feels slow, then propose a concrete next-step plan toward Google-Sheets-level responsiveness.
Findings: The current drag path is no longer blocked by persistence, but the grid body is still a virtualized HTML table in `src/components/tasks/task-workspace.tsx`; each drag frame updates every visible cell shell in the active row, broadcasts `liveRowHeight` through the global layout snapshot, and rebuilds body window math, so the main thread still pays table-layout and body-recompute cost during direct manipulation.
Why: React 19 features (`useSyncExternalStore`, `startTransition`, `useDeferredValue`) are already present; the remaining bottleneck is rendering substrate and subscription granularity, not an outdated framework version. Google Sheets-style tech such as WasmGC improves calc throughput, but row-resize latency here is a DOM/layout problem.
Plan: Stop micro-optimizing the current `<table>` path; build a `DailyGridBodyV2` spike with a `div`/`role="grid"` body plus `@tanstack/react-virtual`, move row metrics into selector-level subscriptions, let one row wrapper own height during drag, keep DOM overlay editors, and reopen a canvas-backed body spike only if the new DOM grid still misses target after measurement.
Verify: Reviewed `docs/worklogs/2026-04-09-spreadsheet-grid-handoff-status.md`, `docs/2026-04-08-grid-latency-remediation-methodology.md`, `docs/worklogs/2026-04-07-daily-row-resize-fast-path.md`, `src/components/tasks/task-workspace.tsx`, `src/app/globals.css`, `src/domains/task/daily-list.ts`, and current official references for TanStack Virtual, Glide Data Grid, AG Grid DOM virtualization, OffscreenCanvas/Web Workers, and Google Sheets WasmGC.
Risk: This is analysis only; no profiler trace or replacement body implementation was run in this task, so the exact split between table-layout cost and body-window recompute cost remains an informed code-level diagnosis rather than a measured flamechart.
