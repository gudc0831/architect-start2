Req: Resume credentialed production operations validation from Slice 482 for the SaaS DB, embedding, and authenticated route gates.
Diff: Updated `scripts/lib/cloud-guard.ts` so cloud row counts run sequentially, fall back to direct `pg` counts if Prisma row reads fail, and treat unknown cloud row counts as non-empty for guarded mutation safety; applied the approved `db:migrate:safe` path and recorded the current configured cloud DB, embedding worker, chunk debug, data-guard readout, and operator-session blockers for the cross-repo closeout.
Why: Production operations cannot resume safely until the non-empty configured cloud DB receives the pending assistant/file-analysis migrations, `file_analysis_chunks` exists, provider/key/write-audit gates are approved, and authenticated runtime routes stop failing on the target branch.
Verify/Time: 2026-05-18 10:05-10:30 KST, refreshed 10:21-10:43 KST; `npm run data:backup` created cloud backup `cloud-2026-05-18T01-21-14-268Z-98d94b0d`; direct read-only SQL initially confirmed the DB was non-empty and lacked `file_analysis_chunks`, `assistant_audit_events`, `vector`, and the four target `_prisma_migrations`; after cloud guard hardening, `npm run typecheck` and `npm run lint` passed; the first approved `npm run db:migrate:safe` issued confirmation token `b5b11e44f137`, and the confirmed run applied all four pending migrations and created backup `cloud-2026-05-18T01-38-24-479Z-b28340f1`; post-migration `npm run data:doctor` reports migration status `ok=true`, populated cloud `rowCounts`, `isNonEmpty=true`, latest backup, and no write lock; direct read-only SQL confirms `file_analysis_chunks=0`, `assistant_audit_events=0`, `vector` extension `0.8.0`, and the four target migrations `finished=true`; `npm run file-analysis:embedding:plan -- --json --sample-limit 0` reports database available with `totalChunks=0` and provider `disabled`; `npm run file-analysis:chunks:debug -- --json --sample-limit 0` reports database available, no blockers, and no rows; `npm run file-analysis:embedding:worker -- --json --sample-limit 0` is blocked only by provider/key absence with selected/updated `0`; `npm run file-analysis:embedding:worker -- --execute --json --sample-limit 0` exits `1` as designed with provider/key/write-audit blockers; authenticated Chrome validation on the Vercel branch reaches `/admin/knowledge` and renders the Knowledge Admin shell, while `/daily` remains at workspace loading with `로그아웃` visible.
