Req: Resume credentialed production operations validation from Slice 482 for the SaaS DB, embedding, authenticated route gates, and stale idle session cleanup.
Diff: Updated `scripts/lib/cloud-guard.ts` for guarded cloud row-count safety; applied the approved `db:migrate:safe` path; cleaned `postgres`/`Supavisor` idle sessions through Supabase MCP; capped SaaS runtime Prisma/pg pool usage via `DATABASE_POOL_MAX`, defaulting to 1 when unset.
Why: Production operations cannot resume safely until the non-empty configured cloud DB receives the pending assistant/file-analysis migrations, `file_analysis_chunks` exists, provider/key/write-audit gates are approved, authenticated runtime routes stop failing, and one-time stale idle cleanup is replaced by a durable deployed pool cap.
Verify/Time: 2026-05-18 10:05-11:24 KST; approved `npm run db:migrate:safe` applied four pending migrations and backup `cloud-2026-05-18T01-38-24-479Z-b28340f1`; escalated `npm run data:doctor` reports schema up to date, rowCounts `profiles=8/projects=2/tasks=2/files=0/preferences=2`, `isNonEmpty=true`, no write lock; escalated embedding plan/chunk debug/worker dry-run/execute paths see DB available and remain blocked only by missing provider/key/write-audit and zero chunks; authenticated Chrome reaches `/admin/knowledge`, current Vercel deployment still blocks `/daily` and is stale at SHA `9ec451b60a4cc003ec84aa02920b1a781a6850cc`; MCP cleanup terminated target idle sessions and final aggregate showed non-saturated `postgres`/`Supavisor` idle sessions; `npm run typecheck`, `npm run lint`, and `npm run build` passed after the pool cap patch.
