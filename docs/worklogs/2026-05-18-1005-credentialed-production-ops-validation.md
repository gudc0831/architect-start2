Req: Resume credentialed production operations validation from Slice 482 for the SaaS DB, embedding, authenticated route gates, and stale idle session cleanup.
Diff: Updated `scripts/lib/cloud-guard.ts` so cloud row counts run sequentially, fall back to direct `pg` counts if Prisma row reads fail, and treat unknown cloud row counts as non-empty for guarded mutation safety; applied the approved `db:migrate:safe` path; cleaned `postgres`/`Supavisor` idle sessions through Supabase MCP; capped SaaS runtime Prisma/pg pool usage via `DATABASE_POOL_MAX`.
Why: Production operations cannot resume safely until the non-empty configured cloud DB receives the pending assistant/file-analysis migrations, `file_analysis_chunks` exists, provider/key/write-audit gates are approved, authenticated runtime routes stop failing, and one-time stale idle cleanup is replaced by a durable deployed pool cap.
Verify/Time: 2026-05-18 10:05-11:12 KST; `npm run data:backup` created cloud backup `cloud-2026-05-18T01-21-14-268Z-98d94b0d`; approved `npm run db:migrate:safe` applied four pending migrations and created backup `cloud-2026-05-18T01-38-24-479Z-b28340f1`; post-migration `npm run data:doctor` reports migration status `ok=true`, populated cloud `rowCounts`, `isNonEmpty=true`, latest backup, and no write lock; direct read-only SQL confirms `file_analysis_chunks=0`, `assistant_audit_events=0`, `vector` extension `0.8.0`, and target migrations `finished=true`; embedding plan/chunk debug/worker dry-run/execute paths see the DB and remain blocked only by missing provider/key/write-audit and zero rows; authenticated Chrome validation reaches `/admin/knowledge`, while `/daily` remains at workspace loading; MCP cleanup terminated target idle sessions, `/daily` reload refilled them to 15, final cleanup after closing validation tabs left `postgres`/`Supavisor` at 1 idle session, and `npm run typecheck`, `npm run lint`, and `npm run build` passed after the Prisma/pg pool cap patch.
