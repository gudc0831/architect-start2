Req: Resume credentialed production operations validation from Slice 482 for the SaaS DB, embedding, and authenticated route gates.
Diff: Updated `scripts/lib/cloud-guard.ts` so cloud row counts run sequentially and fall back to direct `pg` counts if Prisma row reads fail; recorded the current configured cloud DB, embedding worker, chunk debug, data-guard readout, and operator-session blockers for the cross-repo closeout.
Why: Production operations cannot resume safely until the non-empty configured cloud DB receives the pending assistant/file-analysis migrations, `file_analysis_chunks` exists, provider/key/write-audit gates are approved, and authenticated runtime routes stop failing on the target branch.
Verify/Time: 2026-05-18 10:05-10:30 KST, refreshed 10:21-10:36 KST; `npm run data:backup` created cloud backup `cloud-2026-05-18T01-21-14-268Z-98d94b0d`; direct read-only SQL confirms the DB is non-empty (`profiles=8`, `projects=2`, `tasks=2`, `files=0`, `profile_preferences=2`) and lacks `file_analysis_chunks`, `assistant_audit_events`, `vector`, and the four target `_prisma_migrations`; after cloud guard hardening, `npm run typecheck` and `npm run lint` passed, and `npm run data:doctor` reports populated cloud `rowCounts`, `isNonEmpty=true`, latest backup, and no write lock; `npm run file-analysis:embedding:plan -- --json --sample-limit 0`, `npm run file-analysis:embedding:worker -- --json --sample-limit 0`, and `npm run file-analysis:chunks:debug -- --json --sample-limit 0` all report `42P01 relation "file_analysis_chunks" does not exist`; a parallel worker check briefly hit Supabase pooler `EMAXCONNSESSION`, so the worker was rerun sequentially; `npm run file-analysis:embedding:worker -- --execute --json --sample-limit 0` exits `1` as designed with provider/key/write-audit plus schema blockers; authenticated Chrome validation on the Vercel branch reached `/board` and `/daily` with `로그아웃` visible, `/admin/knowledge` reproduced server error digest `1826282582`, and `/daily` remained at workspace loading.
