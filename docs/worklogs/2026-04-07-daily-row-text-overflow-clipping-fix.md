Req: Fix the daily list row UX so shrinking a row no longer lets long title/detail text paint outside the row, and verify the real click/double-click/resize flow in the browser with the same interactions a user performs.
Diff: `src/app/globals.css` now makes `.sheet-table__cell-shell` a constrained flex column and clamps `.sheet-table__cell-content` with `flex: 1 1 auto`, `min-height: 0`, `height: 100%`, and `overflow: hidden`, while keeping editable cell content stretch behavior intact.
Why: Long wrapped text in the daily sheet was sizing its inner content block to natural height instead of the resized row height, so reduced rows could show text below the visual row boundary even though the shell had a fixed height.
Verify/Time: `npm run typecheck` (pass); headed Playwright QA on `http://127.0.0.1:3000/daily` confirmed reduced row state no longer shows long text bleeding below the row, single click keeps the row read-only (`editors: 0`), double click focuses the inline editor in-row (`TEXTAREA` active on the clicked title cell), manual row expand to `137px` still renders cleanly, and browser console errors remained at 0; mobile confirmation pass on `/daily` showed no contradictory layout break / 2026-04-07.
