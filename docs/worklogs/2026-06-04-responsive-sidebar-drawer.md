Req: Build the selected workspace sidebar as a default-collapsed responsive rail/drawer in an isolated worktree, with click/hover open and a task-detail-like pin control, stopping before integration.
Diff: Added sidebar manual/hover/pinned state to `ProjectShell`, wrapped `Sidebar` content with a conditional handle, right-edge pin action, and dismissal backdrop, converted the shell CSS from fixed sidebar column to rail/overlay/pinned dock states, and expanded `workspace-navigation-harness` to lock the contract.
Why: `/daily` needs more horizontal room by default while keeping fast access to project navigation and preserving the existing daily navigation guard behavior.
Verify/Time: `npx.cmd tsx scripts/workspace-navigation-harness.ts`; `npx.cmd tsc --noEmit --incremental false`; `npm.cmd run lint` with one pre-existing `project-materials-page.tsx` hook warning; `npx.cmd tsx scripts/daily-editing-responsiveness-verify.ts`; `git diff --check`; `npm.cmd run build`; `npm.cmd run worklog:check`; browser QA on `/preview/daily` for collapsed rail, hover/click open, pin dock, preview calendar navigation, and 390px mobile drawer on 2026-06-04 KST.
