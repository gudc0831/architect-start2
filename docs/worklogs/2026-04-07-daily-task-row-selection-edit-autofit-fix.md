Req: Fix the daily task row regression so single-click only selects, double-click enters inline edit for the clicked cell, and bottom-handle auto-fit/manual row sizing persist only from explicit resize actions.
Diff: `src/components/tasks/task-workspace.tsx` now separates selected-row state from inline-edit row state, moves inline edit entry to cell-level double-click, removes row-level double-click/detail conflicts, and limits row-height persistence to resize/auto-fit flows; `src/app/globals.css` increases the bottom resize handle hit area and visual affordance.
Why: Stop accidental inline-edit/draft activation and passive row-height rewrites on row selection while restoring the discoverable bottom-handle auto-fit interaction and stable custom row-height retention.
Verify/Time: `npm run typecheck` (pass); headed Playwright QA on `http://127.0.0.1:3000/daily` confirmed single-click stays read-only with no `rowHeights` write, double-click opens the clicked row editor, drag-resize persisted `arch-task-001` height `136` across reload/reselect, and bottom-handle double-click auto-fit updated/persisted the same row to `104`; headed Playwright QA on `http://127.0.0.1:3000/preview/daily` confirmed single-click stays read-only with no layout write, double-click opens the clicked row editor, drag-resize persisted `task-101` height `136` across reload/reselect, and bottom-handle double-click auto-fit updated/persisted the same row to `61` across reload / 2026-04-07.
