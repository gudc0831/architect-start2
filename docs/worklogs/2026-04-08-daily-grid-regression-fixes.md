Req: Finish the remaining daily grid plan gaps without regressions, keep inline edit typography the same as viewer state, and restore blank-space deselect.
Diff: Updated [task-workspace.tsx](/D:/architect%20-%20start2/src/components/tasks/task-workspace.tsx) to honor explicit null selection clears, reuse outside-click deselect across workspace clicks, hide active-cell display content under the editor overlay, and copy anchor cell typography into the portal editor. Updated [globals.css](/D:/architect%20-%20start2/src/app/globals.css) so overlay-hidden cells stay hidden and title editors inherit viewer typography.
Why: The previous overlay path still left selection clear stuck because `null` was swallowed by `??`, and the portal editor inherited body styles so edit mode looked heavier than viewer mode.
Verify: `npm run typecheck`; `npm run build`; browser QA on `http://127.0.0.1:3000/preview/daily` confirmed blank-space deselect (`activeRows 1 -> 0`), overlay removal on deselect, title editor typography match (`font-size 14.08px`, `font-weight 400`, `line-height 20.416px` on viewer/editor), row resize (`52px -> 92px` via pointer sequence), and no browser warnings; `/daily` desktop QA confirmed drag handles are present, enabled, and `draggable=true`.
