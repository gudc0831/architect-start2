Req: Implement the umbrella Architect MVP operational gaps after Slice 474 across embedding execution, chunk debug, source-review coverage, legal-source preflight, external sync worker prep, capability migration, and validation.
Diff: Added guarded embedding worker and package script; added chunk debug service/API/script and Knowledge admin UI; preserved existing chunk embeddings during metadata sync; added source-review coverage report/export and production import preflight; added dry-run Approved WIKI sync worker report/API/script; added Knowledge admin capability schema/API; updated user guide and query-embedding handoff.
Why: The MVP had read-only planning surfaces for chunks/legal governance/provider execution, but still needed operator-grade blocked-state reporting, safe mutation gates, export coverage, and debug surfaces before production credentials or external writes are approved.
Verify/Time: 2026-05-15 18:45 KST; `npm run typecheck`, `npm run lint`, and `NEXT_DIST_DIR=.next-build npm run build` passed; `npm run regulation:seed:validate`, `npm run regulation:governance:validate`, and `npm run retrieval:hybrid:validate` passed; `npm run file-analysis:embedding:plan -- --json --sample-limit 0`, `npm run file-analysis:embedding:worker -- --json --sample-limit 0`, and `npm run file-analysis:chunks:debug -- --json --sample-limit 0` completed with blocked reports because the configured DB lacks `file_analysis_chunks` and provider config/key are absent; `npm run file-analysis:embedding:worker -- --execute --json --sample-limit 0` failed intentionally on provider/key/write-audit plus schema blockers; `npm run approved-wiki:sync-worker -- --json` passed without external writes.
