Req: continue the `codex/multi-user-transition` slice by adding Phase 3 request-integrity protection and lifting project-scoped task/file/admin routes off `requireUser()` without expanding into OAuth UI cutover, RLS, or migration work.
Diff: add `src/lib/auth/request-integrity.ts`; extend `src/lib/auth/project-guards.ts` with current-project access resolution; apply same-site Origin/Referer checks to cookie-authenticated POST/PATCH/PUT/DELETE handlers across project/task/file/admin/trash routes; require `requireCurrentProjectAccess` on selected-project task/file/project reads and writes; move `/api/admin/projects/[projectId]/**` routes to `requireProjectManager`.
Why: the locked auth/RBAC contract and the 2026-04-10 security review still left two Phase 3 gaps open: cookie-authenticated state-changing routes had no explicit request-integrity proof, and many project-scoped APIs still relied on authentication alone instead of an explicit project guard level.
Verify: `npm run typecheck` passed; direct `.\node_modules\.bin\eslint.cmd` on the changed auth/api files passed; `npm run lint -- ...` still expands to repo-wide lint and fails only on the pre-existing task UI files `src/components/tasks/daily-grid-body-v2.tsx`, `src/components/tasks/task-inline-editor-overlay.tsx`, `src/components/tasks/task-categorical-fields.tsx`, and `src/components/tasks/task-workspace.tsx`.
Risks: `task-project-context.ts`, RLS/storage policy, migration work, and full Google OAuth UI cutover remain intentionally untouched; any non-browser client hitting the newly protected POST/PATCH/PUT/DELETE routes now needs valid same-site `Origin`/`Referer` behavior or a later CSRF-token path.
