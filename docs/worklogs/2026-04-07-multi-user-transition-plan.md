Req: write a practical multi-user transition plan, keep PLAN.md as a guide, then lock the 1st-phase permission/local-cloud decisions and fold in Google login, `shortName`, admin/global access, non-member assignee handling, Google-Sheets-style collaboration direction, the assignee backfill scope rule, the global uniqueness rule for `shortName`, the concrete local-part extraction/fallback rule, the global alias-pool scope rule, the phase-1 code-constant location for alias configuration, the final release/risk decisions, and the follow-up review fixes needed to remove plan bottlenecks.
Diff: PLAN.md; docs/2026-04-07-multi-user-transition-plan.md; docs/worklogs/2026-04-07-multi-user-transition-plan.md.
Why: reduce rollout bottlenecks by fixing the phase-1 role baseline (`admin / manager / member`), deferring `viewer / editor`, making `cloud` the only real multi-user validation target, defining Google-email identity plus configurable 3-char `shortName` generation, correcting the identity model so the profile key follows the stable auth user id rather than raw email, keeping non-member assignees as historical-only records, explicitly documenting the `admin` bypass path, collapsing provisioning and phase-4 concurrency choices to single implementations, and turning the remaining ambiguous Phase 3/operations language into concrete implementation rules.
Verify/Time: reviewed the updated plan docs in UTF-8, confirmed PLAN.md still acts only as a guide, and verified the detailed multi-user plan now includes the `admin` access exception in Phase 1, the chosen provisioning baseline, fixed Phase 3 backfill actions, the selected Phase 4 implementations (project counter table, reorder version snapshot validation, file version `409` policy), and alias-pool capacity thresholds; no code/test run needed for this documentation-only change | 2026-04-07 15:55-17:40 KST
