Req: Continue the ALRIS-style legal intelligence plan by adding the architect-saas server adapter and admin status panel for verified legal batch audit summaries.
Diff: Added `fetchLegalBatchAuditStatus`, `/api/legal-batch-audit`, `LegalBatchAuditStatusPanel`, `legal-batch-audit:validate`, and server-only env docs for `LEGAL_BATCH_AUDIT_REPORT_PATH` plus `LEGAL_CHANGE_MONITOR_SECRET`.
Why: The browser must see only sanitized refresh readiness/status while the SaaS server alone chooses the configured report path and attaches the verified-legal monitor secret.
Verify/Time: 2026-05-31 KST; RED `npm.cmd run legal-batch-audit:validate` failed on missing service, then missing panel; GREEN passed 20 cases; `npx.cmd tsc --noEmit --incremental false`; `npm.cmd run legal-search:validate`; `npm.cmd run legal-change:validate`; `npm.cmd run legal-change-monitor:validate`; `npm.cmd run task-review:validate`; `git diff --check` passed with LF/CRLF warnings only; multi-agent review PASS after boundary, remediation-redaction, and header-typography fixes.
