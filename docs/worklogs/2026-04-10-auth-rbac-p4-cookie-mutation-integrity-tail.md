Req: continue the `codex/multi-user-transition` auth hardening by closing the remaining cookie-authenticated mutation routes that were still missing explicit request-integrity checks after the project/task/file/admin pass and the Google login UI cutover.
Diff: add `assertRequestIntegrity` enforcement to `src/app/api/auth/logout/route.ts` and the authenticated preference mutation routes `src/app/api/preferences/quick-create-widths/route.ts`, `src/app/api/preferences/task-list-layout/route.ts`, and `src/app/api/preferences/theme/route.ts`.
Why: the auth contract applies request-integrity to cookie-authenticated POST/PATCH/PUT/DELETE routes broadly, not only to project/task/file/admin paths. After the earlier slice, logout and profile-preference PATCH routes were the remaining session-cookie mutations without explicit Origin/Referer protection.
Verify: `npm run typecheck` passed; direct `.\node_modules\.bin\eslint.cmd` on the changed logout/preference routes passed; mutation-route audit now leaves only `src/app/api/auth/login/route.ts` without `assertRequestIntegrity`, which is expected because it is a credential sign-in endpoint rather than a session-cookie-authenticated mutation.
Risks: no browser-level preview verification was run here, so the preference saves and logout flow still need real UI confirmation in preview/cloud mode; the transitional password login endpoint still exists server-side and remains a separate retirement decision.
