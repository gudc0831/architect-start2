Req: continue the `codex/multi-user-transition` branch by moving the user-facing login UI onto the locked Google OAuth path after the request-integrity and guard-split work, without reopening the broader auth/RBAC contract.
Diff: update `src/app/login/page.tsx` to render a cloud Google-login variant instead of the password form; simplify `src/components/auth/login-form.tsx` into a Google OAuth entry card with disabled fallback messaging; add `buildGoogleLoginUrl` in `src/lib/auth/safe-next-path.ts` so login UI continues to preserve only safe same-site `next` targets.
Why: the auth contract says cloud mode must use Supabase Google OAuth as the user-facing login path, while password login may remain only as a transitional utility. The branch still exposed the password form as the main cloud login surface even after the callback/post-login routing had been hardened.
Verify: `npm run typecheck` passed; direct `.\node_modules\.bin\eslint.cmd` on `src/app/login/page.tsx`, `src/components/auth/login-form.tsx`, and `src/lib/auth/safe-next-path.ts` passed; workspace search shows no remaining UI references to `/api/auth/login`.
Risks: the transitional `/api/auth/login` route still exists as a direct server endpoint for controlled utility use and was intentionally not retired in this slice; no preview-cloud browser run was done here, so the end-to-end Google redirect still needs real environment verification.
