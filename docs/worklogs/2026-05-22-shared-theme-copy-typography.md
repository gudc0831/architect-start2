Req: Apply the Apple-theme copy reduction and smaller typography hierarchy to the other themes without copying Apple colors, shapes, or layout treatment.
Diff: Added shared CSS-only presentation rules that hide auxiliary support copy across themes, normalize heading sizes, compact sidebar project/theme controls, stabilize task ID badges, strengthen active nav indicators, align Warm Studio sidebar/header sizing, and apply Apple Soft Fill colors to actual `.status-pill` task status chips while leaving data/API behavior untouched.
Why: Keeping the change visual-only and separate from the Apple-theme commit reduces integration risk while making Classic, Swiss, Productivity, PostHog, and Apple share the same quieter information hierarchy.
Verify/Time: `npm audit --audit-level=moderate` found 0 vulnerabilities; `npm run typecheck`; targeted sidebar ESLint; ExcelJS smoke test; Playwright verified shared copy/heading, sidebar controls, active nav, theme select, task ID badge behavior, Warm Studio sizing, and Apple `.status-pill` Soft Fill colors at 1680x792 and 606x910. Integration recheck on `codex/theme-design-integration`: `git diff --check`; `npm run db:generate`; `npm run typecheck`; `npm run lint`; `npm run build`; `npx tsx scripts/daily-editing-responsiveness-verify.ts`; browser `/daily` filter-chip, horizontal-scroll, Apple visual, and 606px layout smoke test. | 2026-05-25 KST
