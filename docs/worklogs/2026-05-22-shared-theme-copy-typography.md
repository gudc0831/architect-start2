Req: Apply the Apple-theme copy reduction and smaller typography hierarchy to the other themes without copying Apple colors, shapes, or layout treatment.
Diff: Added shared CSS-only presentation rules that hide auxiliary support copy across themes and normalize project/page/card/nav heading sizes while leaving theme tokens, surfaces, borders, radius, and data/API behavior untouched; installed `@playwright/test` for local browser verification; applied audit fixes and overrode transitive `uuid` to keep `exceljs@4.4.0` without the breaking downgrade.
Why: Keeping the change visual-only and separate from the Apple-theme commit reduces integration risk while making Classic, Swiss, Productivity, PostHog, and Apple share the same quieter information hierarchy.
Verify/Time: `npm audit --audit-level=moderate` found 0 vulnerabilities; `npm run typecheck`; ExcelJS workbook writeBuffer smoke test; Playwright Chromium verified 15 route/theme checks for hidden support copy and normalized heading sizes. | 2026-05-22 KST
