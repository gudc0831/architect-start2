Req: Add Korean holiday API integration with official API priority, local fallback, file cache for local development, and calendar UI loading that keeps preview mode offline.
Diff: Added shared holiday types, server holiday route/service with XML parsing and file cache under `LOCAL_DATA_ROOT/cache/holidays`, refactored local holiday provider to expose labeled month data, wired `task-workspace` to load `/api/calendar/holidays` for live calendar intervals while keeping preview/local fallback behavior, and installed `fast-xml-parser`.
Why: The calendar needed a path to reflect official and temporary Korean holidays without making local development or preview mode depend on an external API.
Verify/Time: `npm run typecheck`; targeted `npx eslint` on changed holiday/calendar files; direct route checks on `http://127.0.0.1:3000/api/calendar/holidays`; `npx tsx -` sanity checks for local fallback, fresh cache hit, and stale-cache fallback; browser QA confirmed preview mode made no `/api/calendar/holidays` request while live `/calendar` did and kept holiday highlighting intact; full `npm run lint` currently gets polluted by untracked `.next-review` generated artifacts | 2026-04-06 KST
