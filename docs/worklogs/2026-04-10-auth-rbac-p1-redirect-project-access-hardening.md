Req: start the `codex/multi-user-transition` implementation by closing the first auth/RBAC P1 blockers without expanding into the full OAuth cutover.
Diff: add `src/lib/auth/safe-next-path.ts`, `src/lib/auth/project-guards.ts`, and `src/lib/auth/workspace-entry.ts`; wire safe post-login redirect handling into `middleware.ts`, `src/app/login/page.tsx`, `src/components/auth/login-form.tsx`, `src/app/api/auth/google/route.ts`, `src/app/auth/callback/route.ts`, `src/app/auth/post-login/route.ts`, and `src/app/auth/no-access/page.tsx`; update workspace pages to gate through the new workspace-entry flow; make project selection membership-aware in admin repository/service layers and route callers; require manager/admin access for project rename; harden the project-session cookie.
Why: the branch still allowed unsafe `/login?next=...` redirects and non-admin project selection/rename paths that ignored membership boundaries, both of which were explicit release blockers in the active plan docs.
Verify: `npm run typecheck` passed; targeted `npx eslint ...` over the changed auth/project files passed; full `npm run lint` still fails on pre-existing task UI issues in `src/components/tasks/daily-grid-body-v2.tsx`, `src/components/tasks/task-inline-editor-overlay.tsx`, `src/components/tasks/task-categorical-fields.tsx`, and `src/components/tasks/task-workspace.tsx`.
Risks: full Google OAuth cutover, no-access UX completion, broader project-manager route remapping, and cookie-authenticated mutation integrity checks are still pending follow-up slices.
