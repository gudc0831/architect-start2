Req: Integrate the surviving PostHog worktree into `codex/multi-user-transition` as the fourth persisted theme mode, expose it as `Warm Studio`, preserve preview as `classic`, and delete the no-longer-needed local worktree and branch only after verification.
Diff: Added `posthog` to the theme contract and UI copy; scoped PostHog tokens to `[data-theme="posthog"]`; gated sidebar/workspace chrome changes behind `themeId === "posthog"`; added `docs/2026-04-20-warm-studio-theme-contract.md`; cleaned generated build noise before final cleanup.
Why: Convert a worktree-only visual experiment into a runtime-safe multi-theme mode without regressing the existing three themes or the preview review flow.
Verify/Time: `npm run typecheck` pass; touched-file ESLint 0 errors with existing warnings in `task-workspace.tsx`; full `npm run lint` still fails on baseline repo issues outside this change set; `npm run build` pass; `/preview/{board,daily,calendar,trash}` served and stayed classic; authenticated service-route visual verification remains blocked by Google login in cloud auth mode | 2026-04-20 KST
Compat: reviewed against current branch auth changes (`Restrict transitional password login route` and related preview auth hardening). No overlapping files or contract conflicts were found; `git diff --name-only HEAD` remains limited to Warm Studio theme files plus docs.
