Req: Diagnose user-visible SaaS delays around task loading, database availability, and slow task creation.
Diff: Verified local data stores and Cloud DB health with `data:doctor`; optimized parentless task creation so it no longer loads the full active task list before creating a task; moved default sibling-order calculation into the memory, Firestore, and Postgres repositories; removed duplicate Postgres file queries from task list repository reads; narrowed active task file-summary loading to the selected project; removed a blocking BOM from the already-modified `tsconfig.json` so Turbopack can parse it.
Why: The reported symptoms were not caused by a missing Cloud schema in the current environment. The slow paths were application-level extra reads: task creation serialized multiple independent lookups and loaded every active task even for root tasks, while task listing queried file state more than once or wider than needed.
Verify/Time: 2026-05-20 16:15-16:35 KST. `npm run data:doctor` passed in real network context with Cloud row counts and migrations OK. Temp local-data copy task creation stayed around 80-95ms, indicating local write latency is dominated by data-guard snapshots. Cloud read-only smoke after the patch returned task list 143ms, project file list 133ms, and full `listTasks(active)` 524ms for the selected project. `npm run typecheck`, `npm run lint`, and `npm run build` passed.
