Req: Implement detail-panel attachment open/save actions plus lightweight preview for previewable file types, using the agreed harness-style split between API/storage, UI, and review.
Diff: Added authenticated file content delivery via `src/app/api/files/[fileId]/content/route.ts`, `src/use-cases/file-service.ts`, `src/storage/*.ts`, and `src/domains/file/metadata.ts`; updated `src/components/tasks/task-workspace.tsx`, `src/app/globals.css`, and `src/lib/ui-copy/catalog.ts` so detail-panel attachments always expose `열기`/`저장`, show compact file metadata, and render a single shared preview area for PDF/image/text files.
Why: Local mode did not provide signed download URLs, so the detail panel could not reliably open or save uploaded files; serving authenticated file bytes from the app removes that dependency and keeps preview support intentionally lightweight.
Verify/Time: `npm run typecheck` (pass); `npm run lint` (pass with 3 pre-existing warnings in `src/components/tasks/task-categorical-fields.tsx` only); browser QA on `http://127.0.0.1:3000/daily` with uploaded `pdf/txt/docx`, confirmed detail-panel `열기`/`저장` buttons, preview switching for image/pdf/txt, `Content-Disposition` inline/attachment headers, and cross-project file access returning `404 FILE_NOT_FOUND` / 2026-04-07.
