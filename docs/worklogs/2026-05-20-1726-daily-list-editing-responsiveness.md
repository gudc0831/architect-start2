Req: Make `/daily` inline editing feel spreadsheet-fast for scope/requester/related/status/task fields while preserving authenticated browser verification as the completion gate.
Diff: Added optimistic inline task patch queueing, current-value commit payloads, immediate edit activation before lease resolution, category text fallback for empty option sets, stable empty row file props, localhost OAuth origin preservation, commit-before-focus/outside-pointer handling, task-level idle-debounced patch coalescing, keepalive PATCH saves, and update-safe task title persistence.
Why: Previously edit entry and status/task commits waited on network work, empty category options made several fields uneditable, localhost OAuth could fall back to Preview origin, text edits could be lost when clicking another cell before blur, rapid multi-field edits were serialized into slow/stale PATCH chains, reloads could abort in-flight inline saves, and clearing task text could make the server reject or roll back the merged PATCH.
Verify/Time: `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed on 2026-05-20. Same-host Preview `/daily` verification passed in the logged-in browser session: collaboration scope 22 ms, requester 21 ms, related field 19 ms, task title 22 ms, and status 20 ms visible feedback; edited values persisted after 25 seconds and reload, then restored values also persisted after 25 seconds and reload. Browser console errors were empty; screenshot capture timed out in CDP, so evidence is DOM/timing/reload state.
