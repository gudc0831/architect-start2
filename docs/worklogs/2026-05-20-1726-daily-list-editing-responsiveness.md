Req: Make `/daily` inline editing feel spreadsheet-fast for scope/requester/related/status/task fields while preserving authenticated browser verification as the completion gate.
Diff: Added optimistic inline task patch queueing, immediate edit activation before lease resolution, category text fallback for empty option sets, stable empty row file props, localhost OAuth origin preservation, and a focused verifier.
Why: Previously edit entry and status/task commits waited on network work, empty category options made several fields uneditable, and localhost OAuth could fall back to Preview origin.
Verify/Time: `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed on 2026-05-20; authenticated same-host Preview browser verification is still pending before goal close.
