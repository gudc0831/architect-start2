Req: Implement daily task click/toggle latency fixes without changing the current UI/UX, using harness-style parallel analysis.
Diff: `src/components/tasks/task-workspace.tsx` now routes daily rows through a memoized row boundary and stabilizes selection/save/reorder hot-path callbacks; `src/providers/dashboard-provider.tsx` now exposes stable scope callbacks so selection effects do not refire on unrelated dashboard state churn; `src/use-cases/task-service.ts` fast-paths ordinary PATCH updates without preloading the full active task list unless reparenting is involved.
Why: Reduce selection-triggered full-table rerender work on the client, preserve memo boundaries across dashboard updates, and remove unnecessary list/category loading on common status/checkbox/text updates on the server.
Verify/Time: `npm run typecheck` (pass); `npm run lint` (3 pre-existing warnings in `src/components/tasks/task-categorical-fields.tsx` only); `npm run build` (pass, 2 unrelated Turbopack warnings in `src/lib/data-guard/shared.ts`) / 2026-04-07.
