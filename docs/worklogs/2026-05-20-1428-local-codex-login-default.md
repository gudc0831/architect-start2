Req: Align the assistant execution UI with the plan decision that the default AI execution path is the user's local Codex login, while keeping SaaS API as the managed server-side alternative.
Diff: Updated `/daily` assistant execution mode defaults and labels so Local Codex login is the default, SaaS API is the managed server path, and Mock is explicitly development-only. Renamed the admin assistant screen framing from general execution policy to SaaS API policy. Updated the user guide with the same boundary. After harness and multi-agent review, also passed the visible review instruction through the Local Codex bridge payload.
Why: The plan separates SaaS app/OAuth access from AI execution login. Showing only SaaS API policy in Admin and defaulting `/daily` to Mock made the implemented behavior look inconsistent with the Local Codex login default. The review instruction had to apply equally across SaaS API, Mock, and Local Codex execution.
Verify/Time: 2026-05-20 14:28-15:07 KST. `npm run typecheck`, `npm run lint`, and `npm run build` passed. `npm run ui-copy:validate` completed typecheck/lint/build but failed on the existing translator-status report gap (`output\ui-copy\translator-status.json` missing) and regenerated `docs/UI_COPY_RUN_REPORT.md`. Harness/multi-agent recheck found and fixed the Local Codex instruction propagation gap. Browser skill verification used `APP_BACKEND_MODE=local`, `ALLOW_INSECURE_NON_CLOUD_PRODUCTION=true`, and `PORT=3100`: `/admin/assistant` rendered `AI 실행 정책`, default Local Codex copy, `SaaS API 정책`, and `SaaS API 모드 사용`; `/daily` opened task `001` assistant panel with execution mode selected as `local-codex` / `로컬 Codex 로그인 (기본)`, Local Codex login section visible, SaaS/Mock alternatives visible, no console warn/error logs, and Local Codex health/generate checks stayed on Local Codex with an explicit extension-connection error instead of auto-selecting Mock.
