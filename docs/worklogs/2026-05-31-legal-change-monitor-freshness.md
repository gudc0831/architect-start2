Req: Continue the ALRIS-style legal intelligence plan by surfacing monthly legal-change monitor freshness in the admin legal-change panel.
Diff: Added `LegalChangeMonitorFreshness` to the legal-change response contract, server-side monitor report mapping in `legal-change-service`, `LEGAL_CHANGE_MONITOR_REPORT_PATH` docs, and freshness chips in `LegalChangeMonitorPanel`.
Why: Operators need to see whether the monthly legal-change monitor is current, stale, or missing without exposing monitor secrets, verified-legal internals, report paths, or raw report JSON to browser clients.
Verify/Time: 2026-05-31 KST; RED `npm.cmd run legal-change-monitor:validate` failed on missing monitor freshness, then on review-raised unsafe warning/dateWindow/cadence cases; GREEN passed after fixed 31-day cadence, malformed-report guards, redacted upstream warnings, and UI `unchangedCount`; `npm.cmd run legal-batch-audit:validate`; `npm.cmd run legal-search:validate`; `npm.cmd run task-review:validate`; `npx.cmd tsc --noEmit --incremental false`; `git diff --check` passed with LF/CRLF warnings only; refined secret-like scan found no actual OpenAI key or LAW OPEN DATA `OC=` value shapes; multi-agent spec/code-quality re-reviews PASS.
