Req: Optimize the reviewed task workspace and project/backend paths without breaking functionality, using harness-style parallel execution where useful.
Diff: Reused project bootstrap payloads, collapsed board status aggregation to a single pass, stopped read-only project-selection snapshot writes, moved `data-guard` storage outside the repo, replaced holiday file cache with in-memory runtime cache, and added backend-aware bulk project issue-id sync for rename flows.
Why: Reduce client request waterfalls, repeated render-time scans, local snapshot churn, serverless-hostile filesystem coupling, and rename-time write amplification across local/Postgres/Firestore backends.
Verify/Time: `npm run data:doctor`; `npm run data:backup`; `npm run typecheck`; `npm run lint` (3 existing warnings in `src/components/tasks/task-categorical-fields.tsx`); `NEXT_DIST_DIR=.next-review npm run build` (pass, 2 remaining Turbopack warnings in `src/lib/data-guard/shared.ts`) / 2026-04-07.
