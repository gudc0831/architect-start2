Req: stop `/daily` row reorder from rolling back after reload and prevent reorder sync from replacing the working grid with the full "workspace loading" placeholder.
Diff: removed the local-equality reorder ack, rebased conflicts from a quiet server snapshot, recomputed expectedVersions from the latest task tree, kept the daily grid mounted while rows/journal ops exist, made IndexedDB journal the only live `/daily` reorder server writer while localStorage queue remains fallback, and added sync fetch timeout/stale syncing recovery.
Why: optimistic order is not a server acknowledgement, forced dashboard refresh breaks spreadsheet-like editing, double-writing the same reorder through journal plus legacy queue can self-conflict, and a hung sync request must return to retryable pending instead of staying syncing forever.
Verify: `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed locally; the verification script guards local-equality ack, non-silent 409 refresh, missing daily loading guard, stale expectedVersions, journal/legacy double-write regression, and sync timeout coverage.
Time: 2026-05-22 KST; preview deployment and browser smoke follow after push.
