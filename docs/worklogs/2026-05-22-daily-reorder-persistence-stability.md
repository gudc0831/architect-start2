Req: stop `/daily` row reorder from rolling back after reload and prevent reorder sync from replacing the working grid with the full "workspace loading" placeholder.
Diff: removed the journal reorder fast path that marked a reorder synced from local optimistic equality alone, rebased legacy reorder queue conflicts from a quiet server snapshot instead of `refreshScope({ force: true })`, recomputed reorder expectedVersions from the latest task tree before each persist attempt, and kept the daily grid mounted while active rows or journal operations exist.
Why: optimistic order is not a server acknowledgement; clearing durable pending reorder state before the server write can lose the user's final order on reload. A forced dashboard refresh during reorder conflict also flips the whole workspace back into loading UI, which breaks spreadsheet-like continuous editing.
Verify: `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed locally; the verification script now guards against reintroducing local-equality reorder ack, non-silent 409 refresh, missing daily loading guard, and stale set_sibling_order expectedVersions.
Time: 2026-05-22 KST; preview deployment and browser smoke follow after push.
