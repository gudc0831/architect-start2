Req: stop `/daily` row reorder from rolling back after reload, prevent reorder sync from replacing the working grid with the full loading placeholder, and clear stale failed reorder sync when server state has already converged.
Diff: removed local-equality reorder ack, quiet-rebased conflicts from server snapshots, recomputed expectedVersions from latest task trees, kept the daily grid mounted while rows/journal ops exist, made IndexedDB journal the only live `/daily` reorder writer, added longer retryable sync timeout/manual-failed recovery, added GET-only stale-failed reorder settlement/local discard, gave task mutation routes explicit Vercel duration, lowered Vercel Prisma PG pool pressure/classified Supabase max-session errors as 503, normalized Vercel Supabase session-pool URLs to transaction-pool runtime URLs, made server `set_sibling_order` apply latest desired order on current task versions, changed Postgres order writes to one bulk update, replayed pending reorder journals from stored `orderedTaskIds`, and rendered `/daily` from a local-first task overlay.
Why: optimistic order is not a server acknowledgement, forced dashboard refresh breaks spreadsheet-like editing, double-writing reorder can self-conflict, slow serverless/DB sync must stay retryable instead of becoming fatal `AbortError`, Supabase session pool exhaustion must not become generic 500 UI, Vercel/serverless runtime should use Supavisor transaction pooling per Supabase/Prisma guidance, hung/exhausted sync must expose retry without churn, duplicate `siblingOrder` ties can leave a false failed chip after convergence, stale client version maps must not block a rebased local-first desired-order sync, remote DB row-by-row reorder writes are too slow/fragile, unrecoverable stale local outbox entries need an explicit escape hatch, and reload replay must apply the durable journal command to the latest server snapshot before any next user action.
Verify/Time: `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed locally on 2026-05-22 KST; preview deployment and browser smoke follow after push.
