Req: continue the `codex/multi-user-transition` hardening after the auth slices by tightening public diagnostics exposure and adding the deployment-contract security header baseline.
Diff: remove `/api/system/status` from middleware public API allowlist; require auth plus `Cache-Control: no-store` in `src/app/api/system/status/route.ts`; add global `Content-Security-Policy`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `Permissions-Policy` headers in `next.config.ts`.
Why: the locked auth and deployment contracts forbid descriptive public diagnostics in production and require a verifiable runtime header baseline beyond the earlier auth/OAuth hardening work.
Verify: `npm run typecheck`; direct `.\node_modules\.bin\eslint.cmd middleware.ts next.config.ts src/app/api/system/status/route.ts`; `npm run build`; runtime check on `http://127.0.0.1:3100/login` confirmed the new headers and unauthenticated `GET /api/system/status` returned `503 CLOUD_ENV_MISSING` instead of detailed system metadata.
Risks: runtime verification was local-only and based on the current env state; authenticated cloud preview verification for the protected diagnostics route is still pending.
