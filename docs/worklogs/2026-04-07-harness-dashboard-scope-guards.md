Req: Execute the confirmed harness plan with parallel agents, improve tab-switch performance without breaking flows, and harden selected-project authorization paths.
Diff: Wrapped `src/components/layout/app-shell.tsx` with the existing `DashboardProvider` so `/board`, `/daily`, `/calendar`, and `/trash` actually share cached dashboard state; added `src/use-cases/project-scope-guard.ts`; updated `src/use-cases/file-service.ts` and `src/use-cases/trash-service.ts` to reject cross-project file/task mutations; adjusted `src/lib/auth/auth-config.ts` and `src/lib/auth/require-user.ts` so unsafe non-cloud production still fails closed on request-bound auth while static build verification stays green.
Why: The previous workspace remounted and re-fetched dashboard data on each tab change, file/trash mutations trusted raw `taskId`/`fileId`, and the first auth guard attempt blocked `next build` prerender instead of only the unsafe production request paths.
Verify/Time: `npm run typecheck` pass; `npm run lint` pass with the same 3 existing warnings in `src/components/tasks/task-categorical-fields.tsx`; `npm run build` pass after auth guard adjustment; browser-verified on 2026-04-07 at `http://127.0.0.1:3000` that `/daily -> /board -> /calendar -> /daily?taskId=arch-task-092` still works and the target task remains selected in the detail panel; mobile check at `390x844` confirmed the daily page still renders without overlap or blank states; Playwright console reported 0 errors and 0 warnings in the final pass.
