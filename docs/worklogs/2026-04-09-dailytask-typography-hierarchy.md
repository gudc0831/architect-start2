Req: Reduce DailyTask header dominance, make date columns a quieter supporting tier, and keep the change scoped to the daily table across virtualized and fallback paths.
Diff: Added shared `data-task-column` markers to DailyTask virtual/fallback cells plus the off-screen measurement shell, scoped the table with `sheet-wrapper--daily` / `sheet-table--daily`, and updated `src/app/globals.css` so daily headers render at 13px, body cells at 14px, issue titles at 15px, and due/review dates at 12px with tabular numerals.
Why: The V2 grid was inheriting page-level 16px typography, so sticky headers and date values visually outranked the main row content instead of supporting it.
Verify: `npm run typecheck`; `npm run build` (passes, with pre-existing Turbopack `data-guard` glob warnings); browser QA on `http://localhost:3000/preview/daily` at 1440, 1024, and 390 widths confirmed header labels at 13px, body cells at 14px, issue titles at 15px, due/review dates at 12px, no new console errors, and no mobile-card typography regression.
