Req: Stabilize `/daily` cloud-mode entry and make task creation/deletion feel spreadsheet-fast, especially showing newly-created tasks in the task list without waiting for the server round trip.
Diff: Added dashboard read retries and non-blocking system-status loading, reduced duplicate project/task lookups, classified transient database failures, added cloud latency verification, and changed task creation/deletion/restore flows to update loaded dashboard task lists optimistically.
Why: The prior flow could surface transient cloud failures as generic red banners and made users wait for server refreshes even when the UI already had enough local state to update the affected task row immediately.
Verify/Time: 2026-05-21 KST. `npm run typecheck`, `npm run lint`, `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run build`, and elevated `npx tsx scripts/daily-cloud-latency-verify.ts --max-read-ms 3000 --mutate` passed; final authenticated browser signoff is deferred to the user.
