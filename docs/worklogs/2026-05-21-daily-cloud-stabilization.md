Req: Stabilize `/daily` cloud-mode entry and make creation, editing, reorder, status, file, and trash actions feel spreadsheet-fast before server round trips finish.
Diff: Added dashboard read retries/non-blocking status loading, reduced duplicate lookups, classified transient DB failures, added cloud latency verification, and made task create/save/reorder/status/trash plus file upload/version/remove/restore/delete/bulk-trash actions update local dashboard state optimistically with rollback. Reorder now persists through a background queue so move buttons and drag handles remain available while prior order writes are still in flight.
Why: The prior flow could surface transient cloud failures as generic red banners and made users wait for server refreshes even when the UI already had enough local state to update the affected row/card/file pill immediately. A later review found a remaining global reorder pending flag that made chained task movement impossible before server acknowledgement.
Verify/Time: 2026-05-21 KST. `npm run typecheck`, `npm run lint`, `npx tsx scripts/daily-editing-responsiveness-verify.ts`, `npm run build`, and earlier elevated `npx tsx scripts/daily-cloud-latency-verify.ts --max-read-ms 3000 --mutate` passed after the optimistic queue update; final authenticated browser signoff is deferred to the user.
