Req: Add explicit calendar month context plus month navigation and direct month jumping so users can tell which month is shown and move to another month from the calendar tab.
Diff: `src/components/tasks/task-workspace.tsx` now resolves a shared `activeCalendarMonth` from `month`, `taskId`, today, and scheduled tasks, then uses it for both month-grid and agenda rendering; added previous/today/next controls, a month picker, and selected-month empty states; updated `src/lib/ui-copy/catalog.ts` and `src/app/globals.css` for the new copy and layout.
Why: The calendar previously rendered a month implicitly with no visible month state or direct month navigation, which made task-based planning and date browsing harder than the board view.
Verify/Time: `npm run typecheck`; `npm run lint` (existing warnings only); browser QA on `/preview/calendar` at 1440 month-grid, 1024 agenda breakpoint, and 390 mobile agenda with button/picker navigation, empty-month handling, `?taskId=task-104` default-month behavior, and bare `/preview/calendar` default-to-today behavior; browser console showed only standard Next dev info/HMR logs | 2026-04-06 KST
