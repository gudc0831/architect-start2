Req: Continue the next remaining Architect slice after review clearance by adding the legal-source governance admin UI.
Diff: Added a read-only regulation governance report service, `/api/admin/knowledge/regulation-governance`, and a Knowledge admin panel showing manifest validation, production-import blocking, source refresh status, checklist counts, and a copyable Markdown report.
Why: The regulation governance manifest was CLI-verifiable but not visible to Knowledge admins, leaving legal-source refresh review as an operator-only step.
Verify/Time: 2026-05-15 13:15 KST; `npm run typecheck`, `npm run lint`, `npm run regulation:governance:validate`, and `NEXT_DIST_DIR=.next-build npm run build` passed. Direct `getRegulationGovernanceReport()` check returned package `kr-architecture-foundation-2026-05-14`, 6 sources, 7 documents, 0 errors, and 0 warnings. Local `next start` HTTP check confirmed `/admin/knowledge` redirects to login and the unauthenticated governance API returns 401.
