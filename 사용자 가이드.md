# Architect Browser Assistant 사용자 가이드

작성일: 2026-05-07

## 바로 테스트하기

현재 바로 테스트 가능한 기본 경로는 SaaS 일일 목록 화면입니다.

```powershell
cd D:\architect-workspace\architect-saas
npm run dev
```

브라우저에서 다음 주소를 엽니다.

```text
http://localhost:3000/daily
```

## 일일 목록 assistant 흐름

1. `/daily`에 접속합니다.
2. 일일 목록에서 task row를 클릭합니다.
3. 화면 오른쪽 아래의 `AI 검토` 버튼을 누릅니다.
4. 질문과 검토 지침을 확인하거나 수정합니다.
5. `근거 조회 + 의견 생성`을 눌러 선택 task 기준 근거 조회, mock 검토 의견 생성, assistant record 저장을 한 번에 실행합니다.
6. `작업 기록 승인`을 눌러 작업 기록 정리 초안을 승인합니다.

이 기능은 PC 포인터 환경을 우선 대상으로 합니다. 모바일 assistant UX는 후속 확장 범위입니다.

## SaaS API Mode 테스트 흐름

05 SaaS API Mode부터 로컬/mock 생성 외에 서버 정책 기반 실행 모드를 테스트할 수 있습니다. `mock` provider는 deterministic 응답을 생성하고, `openai` provider는 서버에 `OPENAI_API_KEY`가 설정된 경우에만 live provider를 호출합니다.

### `/daily` PC 팝업에서 테스트

1. `/daily`에서 task row를 클릭합니다.
2. `AI 검토`를 눌러 assistant 팝업을 엽니다.
3. `실행 모드`를 `SaaS API`로 변경합니다.
4. `SaaS API Mode`가 `enabled`인지 확인합니다.
5. `근거 조회 + 의견 생성`을 누릅니다.
6. 검토 의견 하단의 사용량 문구와 저장 완료 상태를 확인합니다.

정책이 disabled이면 생성은 차단되고, 사용량 차단 이벤트와 감사 이벤트가 남습니다.

### 관리자 운영 화면에서 정책/리포트 확인

```text
http://localhost:3000/admin/assistant
```

1. `/admin/assistant`에 접속합니다.
2. 현재 프로젝트의 SaaS API Mode enabled 상태, provider, model, token 제한, 월 예산, 허용 evidence type을 확인합니다.
3. 필요하면 provider를 `mock` 또는 `openai`로 바꾸고 `정책 저장`을 누릅니다.
4. 월별 리포트에서 성공/차단/실패 요청, input/output token, 예상 비용, budget 사용률을 확인합니다.
5. Audit timeline에서 정책 변경, 생성 성공, 차단, provider 실패 이력을 확인합니다.

`openai` provider를 선택했는데 서버에 `OPENAI_API_KEY`가 없으면 생성 요청은 실패로 기록되고, 브라우저에는 안전한 설정 오류만 표시됩니다.

### `/assistant-test`에서 테스트

1. `/assistant-test`에 접속합니다.
2. `Enable SaaS policy`를 눌러 현재 프로젝트의 SaaS API Mode를 켭니다.
3. `SaaS Generate + Save`를 누릅니다.
4. 답변, draft summary, usage estimate, assistant record 저장 상태를 확인합니다.

관련 API:

- `GET /api/assistant/policy`
- `POST /api/assistant/generate`
- `GET /api/admin/assistant/policy`
- `PUT /api/admin/assistant/policy`
- `GET /api/admin/assistant/usage?month=YYYY-MM`
- `GET /api/admin/assistant/audit?month=YYYY-MM`

## 첨부 파일 근거 저장 흐름

03 File/Image Analysis slice부터 `/daily` assistant 팝업에서 task 첨부 파일 근거를 저장할 수 있습니다.

1. `/daily`에서 첨부 파일이 있는 task를 선택합니다.
2. `AI 검토`를 눌러 assistant 팝업을 엽니다.
3. `파일 근거` 영역에서 첨부 파일을 선택합니다.
4. `확인한 내용`에 도면 메모, PDF 본문, OCR 결과, 이미지 영역에서 확인한 텍스트를 입력합니다.
5. `요약`에 assistant 근거 목록에 표시할 짧은 요약을 입력합니다.
6. `파일 근거 저장`을 누릅니다.
7. 다시 `근거 조회 + 의견 생성`을 누르면 저장된 파일 근거가 `project_document` evidence로 반영됩니다.

사용자가 확인하지 않은 OCR/이미지 근거는 낮은 신뢰도 단서로만 사용됩니다. 최종 판단이나 공식 지식 승격 전에는 사용자가 내용을 확인해야 합니다.

## Knowledge admin WIKI 후보 검토 흐름

assistant 답변과 작업 기록 정리 초안은 관리자 검토 후보로 이어집니다.

```text
http://localhost:3000/admin/knowledge
```

1. `/admin/knowledge`에 접속합니다.
2. `후보 목록`에서 검토 대기 항목을 선택합니다.
3. 원문 질문, 답변, 근거, 신뢰도, task/project 맥락을 확인합니다.
4. `WIKI Draft`의 제목, 요약, 태그, Markdown 본문, 공개 범위를 수정합니다.
5. 공식 지식으로 쓸 항목은 `WIKI 지식 승인`을 누릅니다.
6. 부적합한 항목은 반려 사유를 입력하고 `반려`를 누릅니다.

승인된 항목은 현재 slice에서 assistant record metadata에 retrieval-ready WIKI draft로 저장됩니다. 별도 중앙 WIKI 테이블과 사용자용 WIKI 화면은 후속 확장입니다.

## Harness 테스트 흐름

SaaS 내부 테스트 화면도 유지됩니다.

```text
http://localhost:3000/assistant-test
```

1. `/assistant-test`에 접속합니다.
2. task가 없으면 `Create sample task`를 누릅니다.
3. 질문을 확인하거나 수정합니다.
4. `1. Retrieve Evidence`를 눌러 SaaS task/project 근거를 검색합니다.
5. `2. Mock Generate + Save`를 눌러 mock runtime 답변을 생성하고 assistant record를 저장합니다.
6. `3. Approve Summary`를 눌러 작업 기록 정리본을 승인합니다.

이 흐름은 다음 API를 실제로 호출합니다.

- `GET /api/tasks`
- `POST /api/tasks`
- `POST /api/assistant/retrieve`
- `POST /api/assistant/records`
- `POST /api/assistant/summaries`
- `GET /api/files?taskId=...`
- `POST /api/files/:fileId/analysis`
- `GET /api/files/:fileId/analysis`
- `GET /api/assistant/external-evidence?taskId=...`
- `POST /api/assistant/external-evidence`
- `GET /api/assistant/policy`
- `POST /api/assistant/generate`
- `GET /api/admin/assistant/policy`
- `PUT /api/admin/assistant/policy`
- `GET /api/admin/assistant/usage?month=YYYY-MM`
- `GET /api/admin/assistant/audit?month=YYYY-MM`

## 외부 웹/스킬 근거 저장 흐름

04 Web/Skill Expansion slice부터 `/daily` assistant 팝업에서 사용자가 명시적으로 승인한 외부 자료를 task 근거로 저장할 수 있습니다. 이 기능은 현재 PC 팝업 UX를 우선 지원하며, 모바일 UX는 후속 확장 범위입니다.

1. `/daily`에서 검토할 task row를 클릭합니다.
2. 우측 하단 `AI 검토` 버튼을 눌러 assistant 팝업을 엽니다.
3. `외부 웹/스킬 근거` 영역에서 `사용자 승인 근거로 저장`을 켭니다.
4. 출처 유형을 고르고 제목, 핵심 내용, 출처 URL 또는 스킬/도구 이름을 입력합니다.
5. `외부 근거 저장`을 누릅니다.
6. 다시 `근거 조회 + 의견 생성`을 누르면 저장된 외부 근거가 `web_or_skill` evidence로 포함됩니다.
7. 생성된 assistant record는 외부 출처 URL 또는 tool name을 보존하며, `/admin/knowledge` 후보 검토 화면에서도 근거 출처를 확인할 수 있습니다.

외부 근거는 공식 기준이나 중앙 지식으로 자동 승격되지 않습니다. 관리자 검토 전까지는 신뢰도 낮은 검토 단서로 취급하고, 최종 판단 전에는 원문 URL, 제조사 문서, 발주처 지침, 공개 기준의 최신성을 별도로 확인해야 합니다.

## 현재 구현된 기능

- `/daily` 선택 task에 반응하는 PC 전용 assistant panel
- `/daily` assistant 팝업의 `SaaS API` 실행 모드
- SaaS API Mode admin policy 조회/수정, usage ledger, audit timeline, provider failure 기록
- `/admin/assistant` 정책 및 월별 사용량 리포트 화면
- `/daily` assistant 팝업에서 사용자 승인 외부 웹/스킬 근거 저장 및 `web_or_skill` evidence 반영
- `/admin/knowledge` 관리자 WIKI 후보 검토와 승인/반려 흐름
- SaaS task context 기반 assistant 테스트 화면
- 현재 task와 같은 project의 업무 데이터 검색
- 기존 assistant record 재검색
- 첨부 파일 분석 metadata 저장과 `project_document` evidence 반영
- mock runtime 답변 생성
- SaaS API Mode mock provider 응답 생성 및 OpenAI live provider adapter
- 답변 원문, 근거, confidence score 저장
- 작업 기록 정리 초안 승인/보류 저장
- assistant 신규 테이블 RLS policy
- Chrome extension origin allowlist 환경변수

## 브라우저 확장 테스트

확장 프로그램 build:

```powershell
cd D:\architect-workspace\architect-browser-assistant
npm run build
```

Chrome에서 `chrome://extensions`를 열고 Developer mode를 켠 뒤 `Load unpacked`로 아래 폴더를 선택합니다.

```text
D:\architect-workspace\architect-browser-assistant\dist
```

확장 프로그램이 SaaS mutation API를 호출하려면 SaaS 실행 환경에 설치된 extension origin을 등록해야 합니다.

```powershell
$env:ARCHITECT_ASSISTANT_EXTENSION_ORIGINS="chrome-extension://<extension-id>"
npm run dev
```

## 아직 mock인 부분

현재 local ChatGPT/Codex bridge는 아직 실제 연결되지 않았습니다.

- `MockAssistantRuntime`은 테스트용 답변을 생성합니다.
- `LocalRuntimeClient`는 unavailable 상태를 반환합니다.
- 실제 local ChatGPT/Codex runtime bridge는 다음 goal에서 구현해야 합니다.

## 문제 해결

- `/assistant-test`가 로그인 화면으로 이동하면 SaaS auth 설정을 확인합니다. local stub 모드에서는 바로 접근 가능합니다.
- `Create sample task`가 실패하면 현재 project 권한 또는 work type 설정을 확인합니다.
- extension POST가 실패하면 `ARCHITECT_ASSISTANT_EXTENSION_ORIGINS`에 실제 extension origin이 들어갔는지 확인합니다.
- cloud DB에 반영하기 전에는 `npm run data:backup`을 먼저 실행합니다.
